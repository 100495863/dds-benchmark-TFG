# DDS Benchmark – Common Components

This directory contains common definitions used by all DDS implementations:

- IDL message definition
- Timestamp utilities
- CPU and memory measurement scripts

All DDS implementations must use this message format and timing logic.
